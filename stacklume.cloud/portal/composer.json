{
    "name": "stacklume/portal",
    "description": "STACK Vault portal — PHP runtime dependencies.",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "stripe/stripe-php": "^14.0"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
